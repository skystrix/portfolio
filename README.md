# portfolio

Plain HTML/CSS, no build step.

Run locally: `python3 -m http.server` then open http://localhost:8000

New blog post: copy `blog/hello-world.html` to `blog/<slug>.html`, edit, then add a line at the top of the list in `blog/index.html`.
